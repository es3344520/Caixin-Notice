{
  "name": "my-vercel-api",
  "version": "1.0.0",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "start": "vercel preview"
  },
  "dependencies": {
    "@vercel/node": "^2.0.0"
  }
}
